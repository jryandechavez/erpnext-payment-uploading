[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "erpnext_payment_uploading"
authors = [{name = "Tic and Terry"}]
description = "Review cheque allocations from Excel and create ERPNext Payment Entries"
requires-python = ">=3.10"
dynamic = ["version"]
dependencies = []

[tool.bench.frappe-dependencies]
frappe = ">=15.0.0,<16.0.0"
erpnext = ">=15.0.0,<16.0.0"
